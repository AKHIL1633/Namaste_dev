//Explore about the attribute of JSX
//Explore how to insert image and hyperlink in it


// npx parcel index.html 
npx means you are executing the npm package and exectuting the index.html
it will create the development buid 

//it is better to create the scripts instead of it 

"scripts": {
    "start":"parcel index.html",
    "build":"parcel build index.html",
    "test": "jest "

  },

npm run start --- behind the scence calling the parcel index.html
npm run build 

//Jsx understand Ecma script language 

//Parcel is doing the job behind the scene 

// When you write the code ,go to the js engine it is transpiled before it goes js engine ,js engine  will make it understanable 
//such that that browser can understand

//transpiled means browser can understand 

//Jsx (transpiled before it reaches the Js )-- Parcel(it has own cabinte of mInister)-> Babel(a package parcel install it )

//Babel  is A javascript compiler takes your jsx and converts it to the code that the js will understand (it is not created by facebook)
