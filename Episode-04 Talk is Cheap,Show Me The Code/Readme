in the header we will have logo ,home ,about and cart

in the body section we will search button and cards 

in the footer we will compay links 

 
 * header
   * -logo 
   * - Nav Items

 *Body 
  -Search 
  -Restaturant Container 
      -Restaturant Card 
        -- iMG
        --Name of Res,Star Rating,cuisines ,Delivery time etc 


*Footer
*--- > Copyright
    -> Links 
    ->Addresss
    ->Contact


const styleCard={
  backgroundColor:"grey"
}

// Restaturant card
// Way to write the inline style take the javascript object 

<!-- const RestaturantCard =() =>{
  return (
    <div className="res-card" style={styleCard}>
      <h2>Meghana Foods</h2>
      </div>
  )
} - -->



<!-- const RestaturantCard =() =>{
  return (
    <div className="res-card" style={
    {
    backgroundColor:"#f0f0f0",
    }}>
      <h2>Meghana Foods</h2>
      </div>
  )
} -->


on hover will create the border ,when we move the mouse 

Now we will see how wecan get the card Dynamics 

props is a short form of property which we can pass it to the component

Funtional compontent is a javascript function 
props are just normal argument to the function

// to make it dynmaic we need to pass the value 

<!-- const Body =() =>{
  return (
    <div className="body">
      <div className="Search">Search</div>
      <div className="res-container">
      <RestaturantCard 
      resName="Meghana Foods" 
      cuisine="Biryani,North Indian ,Asian"/>
      
      <RestaturantCard resName="KFC" cuisine="Burger ,Fast Food ,Asian"/>
      


     </div>
    </div>
  ) -->


  Props is an Javascript object 

Desrtructuring of the props 

  <!-- const RestaturantCard =({resName,cuisine}) =>{
  console.log(props)
  return (
    <div className="res-card" style={
    {
    backgroundColor:"#f0f0f0",
    }}>
    <img className="res-logo" alt=" " src="https://media-assets.swiggy.com/swiggy/image/upload/fl_lossy,f_auto,q_auto,w_660/RX_THUMBNAIL/IMAGES/VENDOR/2024/9/17/c8560e2d-defb-437e-a4af-169a700b7ac0_574056.ss.jpg"/>
      <h2>{resname}</h2>
      <h4>{cuisine}</h4>
      <h4>4.4 stars </h4>
      </div>
  )
}
// we can many cards in the Body 

const Body =() =>{
  return (
    <div className="body">
      <div className="Search">Search</div>
      <div className="res-container">
      <RestaturantCard 
      resName="Meghana Foods"
      cuisine="Biryani,North Indian ,Asian"/>

      <RestaturantCard resName="KFC" cuisine="Burger ,Fast Food ,Asian"/>
      


     </div>
    </div>
  )
} -->


// 2 nd way 




<!-- const RestaturantCard =({props}) =>{
  const {resName,cuisine}=props;
  return (
    <div className="res-card" style={
    {
    backgroundColor:"#f0f0f0",
    }}>
    <img className="res-logo" alt=" " src="https://media-assets.swiggy.com/swiggy/image/upload/fl_lossy,f_auto,q_auto,w_660/RX_THUMBNAIL/IMAGES/VENDOR/2024/9/17/c8560e2d-defb-437e-a4af-169a700b7ac0_574056.ss.jpg"/>
      <h2>{resname}</h2>
      <h4>{cuisine}</h4>
      <h4>4.4 stars </h4>
      </div>
  )
} -->


// map iterate 

//we must use key otherwise it will be difficult to reload it 
//react itself say never used index (as it is a bad practice )
//not using key(not acceptable ) unique key is the best practics >>>> index as key 

<!-- <div className="res-container">
  {
    resObj.map((restaurant) => (
      <RestaturantCard 
        key={restaurant.info.id}
        resData={restaurant} 
      />
    ))
  }
</div> -->